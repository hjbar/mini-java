J'ai commencé la compilation.

J'ai essayé de faire la compilation des valeurs constantes, les opérations arithmétiques etc... sauf l'add des strings et le string_of_int.
J'ai essayé aussi de faire les stmts.
J'ai aussi commencé les variables locales.
J'ai essayé également la compilation des méthodes.

==> Là tout de suite il faut faire le système pour les classes.
    C'est-à-dire les 3 étapes :
      - build the class descriptors
      - set the offsets of attributes (within objects) and local variables (within the stack)
      - compile the body of methods and constructors
    Là dedans j'ai déjà bien commencé de mettre à jour l'offset des variables locales et aussi la compilation des méthodes.
