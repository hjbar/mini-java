À faire (+ court terme) :

- Peut-être remplacer la variable globale pour stocker this par le stocker dans la hashtbl
- Vérfier que le main existe bel et bien (peut-être fait au parsing ?)
- Regarder si les variables sont bien init quand on les utilise
- s'occuper des returns
- s'occuper des scopes (avec les blocks)


À faire (+ long terme) :

- Intégrer l'héritage
- Intégrer le sous-typage
